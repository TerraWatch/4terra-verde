import { Typography } from '@mui/material';

export const About = (): JSX.Element =>
{
	return (
        <>
            <Typography variant='h6' style={ { textAlign: 'center' } }>About</Typography>
        </>
    )
}