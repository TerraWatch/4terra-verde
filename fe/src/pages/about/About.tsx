import { Typography } from '@mui/material';
import Styles from './Styles';

export const About = (): JSX.Element =>
{
	return (
        <>
            <Typography variant='h6' style={ Styles.Title }>About</Typography>
        </>
    )
}