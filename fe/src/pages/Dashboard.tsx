import { Typography } from '@mui/material';

export const Dashboard = (): JSX.Element =>
{
	return (
        <>
            <Typography variant='h6' style={ { textAlign: 'center' } }>Dashboard</Typography>
        </>
    )
}