import { Typography } from '@mui/material';
import Styles from './Styles';

export const Dashboard = (): JSX.Element =>
{
	return (
        <>
            <Typography variant='h6' style={ Styles.Title }>Dashboard</Typography>
        </>
    )
}