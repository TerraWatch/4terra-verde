import CSS from 'csstype';

const boxStyling: CSS.Properties = { bottom: 0, position: 'absolute', width: '100%', backgroundColor: '#1976d2' };
const titleStyling: CSS.Properties = { flex: 1, cursor: 'pointer' };

const Styles =
{
    Box: boxStyling,
    Title: titleStyling
}

export default Styles;